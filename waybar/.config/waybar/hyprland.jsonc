{
  // -------------------------------------------------------------------------
  // Per-host overlay
  // -------------------------------------------------------------------------
  // WAYBAR_HOST is set by waybar@.service from systemd's %l (short
  // hostname). Objects merge key by key and the FIRST definition wins, so
  // this file beats hosts/<name>.jsonc: a host file supplies only keys
  // this one leaves unset. Verified rather than assumed — height 24 here
  // against 40 in an include logs "Bar configured (height: 24)".
  //
  // So anything a host has to differ on cannot be set here at all, and
  // group/hardware is the case: only mau5 has a readable GPU sensor, so
  // each host file owns its own copy of that list. A host with no file
  // comes up without the group.
  "include": ["~/.config/waybar/hosts/$WAYBAR_HOST.jsonc"],

  // -------------------------------------------------------------------------
  // Global configuration
  // -------------------------------------------------------------------------
  "layer": "bottom",
  "reload_style_on_change": true,

  // If height property would be not present, it'd be calculated dynamically
  "height": 24,

  "position": "bottom",
  "spacing": 4,

  // -------------------------------------------------------------------------
  // Modules
  // -------------------------------------------------------------------------

  "battery": {
    // Pin to a specific name so auto-discovery doesn't scan transient
    // batteries like Bluetooth controllers. Waybar has a known crash
    // (terminate with std::runtime_error from inotify_add_watch) when
    // a hot-plugged battery sysfs entry disappears mid-session — e.g.
    // a DualSense controller battery at
    // /sys/class/power_supply/ps-controller-battery-XX:XX:XX:XX:XX:XX
    // vanishing when the controller disconnects. "BAT0" doesn't exist
    // on this desktop so the module gracefully shows nothing.
    // "bat": "BAT0",
    "interval": 30,
    "states": {
      "warning": 30,
      "critical": 15,
    },
    "format-charging": "󰚥 {capacity}% ({time})",
    "format": "{icon} {capacity}% ({time})",
    "format-time": "{H}h{M}m",
    "format-icons": ["󰂃", "󰁺", "󰁻", "󰁼", "󰁽", "󰁾", "󰁿", "󰂀", "󰂁", "󰂂", "󰁹"],
    "tooltip": true,
  },

  "idle_inhibitor": {
    "format": "{icon}",
    "format-icons": {
      "activated": "󰒲",
      "deactivated": "󰒳",
    },
    "tooltip": true,
    "tooltip-format-activated": "active",
    "tooltip-format-deactivated": "idle",
    "signal": 15,
  },

  "tray": {
    "icon-size": 18,
    "spacing": 4,
  },

  "custom/pacman": {
    "format": "󰏔 {}",
    "interval": 1800,
    "exec-if": "[ $(pamac checkupdates -q | wc -l) -gt 0 ]",
    "exec": "pamac checkupdates -q | wc -l",
    "on-click": "pamac-manager --updates; waybar-signal.sh pacman",
    "signal": 14,
  },

  "custom/recorder": {
    "interval": 5,
    "return-type": "json",
    "format": "{}",
    "exec": "~/.config/wayland/scripts/recorder.py status",
    "exec-if": "~/.config/wayland/scripts/recorder.py is-recording",
    "on-click": "~/.config/wayland/scripts/recorder.py toggle",
    "signal": 8,
  },

  "custom/stt": {
    "interval": 3,
    "return-type": "json",
    "format": "{}",
    "exec": "~/.config/wayland/scripts/speech.py stt status",
    "exec-if": "~/.config/wayland/scripts/speech.py stt is-recording",
    "on-click": "~/.config/wayland/scripts/speech.py stt toggle --output clipboard",
    "signal": 10,
  },

  "custom/tts": {
    "interval": 3,
    "return-type": "json",
    "format": "{}",
    "exec": "~/.config/wayland/scripts/speech.py tts status",
    "exec-if": "~/.config/wayland/scripts/speech.py tts is-speaking",
    "on-click": "~/.config/wayland/scripts/speech.py tts toggle",
    "signal": 13,
  },

  "custom/copywriter": {
    "interval": 3,
    "return-type": "json",
    "format": "{}",
    "exec": "~/.config/wayland/scripts/copywriter.py status",
    "exec-if": "~/.config/wayland/scripts/copywriter.py is-running",
    "signal": 11,
  },
  "group/extras": {
    "orientation": "horizontal",
    "modules": [
      "custom/pacman",
      "custom/playerctl",
      "hyprland/language",
    ],
  },

  "group/connection": {
    "orientation": "horizontal",
    "modules": ["bluetooth", "network"],
  },

  "group/status": {
    "orientation": "horizontal",
    "modules": [
      "pulseaudio",
      "backlight",
      "battery",
      "custom/notification",
      "clock",
    ],
  },

  "backlight": {
    "format": "{icon} {percent}%",
    "format-icons": ["󰃞", "󰃟", "󰃠"],
  },

  "custom/playerctl": {
    "interval": 5,
    "tooltip": true,
    "return-type": "json",
    "format": "{icon}",
    "format-icons": {
      "Playing": "",
      "Paused": "",
    },
    "exec": "playerctl --player=spotify metadata --format '{\"alt\": \"{{status}}\", \"tooltip\": \"{{playerName}}:  {{markup_escape(title)}} - {{markup_escape(artist)}}\" }'",
    "on-click": "playerctl play-pause; waybar-signal.sh playerctl",
    "on-scroll-up": "playerctl next; waybar-signal.sh playerctl",
    "on-scroll-down": "playerctl previous; waybar-signal.sh playerctl",
    "signal": 5,
  },

  "custom/notification": {
    "tooltip": false,
    "format": "{icon}",
    "format-icons": {
      "notification": "<span foreground='red'><sup></sup></span>",
      "none": "<sup> </sup>",
      "dnd-notification": "<span foreground='red'><sup></sup></span>",
      "dnd-none": "<sup> </sup>",
    },
    "return-type": "json",
    "exec-if": "which swaync-client",
    "exec": "swaync-client -swb",
    "on-click": "swaync-client -t -sw",
    "on-click-right": "swaync-client -d -sw",
    "escape": true,
  },

  // -------------------------------------------------------------------------
  // Hyprland-specific configuration
  // -------------------------------------------------------------------------

  "modules-left": ["hyprland/workspaces"],
  "modules-center": [
    "custom/recorder",
    "custom/stt",
    "custom/tts",
    "custom/copywriter",
    "hyprland/submap",
    "hyprland/window",
  ],
  "modules-right": [
    "group/extras",
    "group/hardware",
    "group/connection",
    "tray",
    "group/status",
  ],

  // -------------------------------------------------------------------------
  // Hyprland-specific modules
  // -------------------------------------------------------------------------

  "hyprland/workspaces": {
    "format": "{icon}: {windows}",
    "format-window-separator": "",
    "on-click": "activate",
    "all-outputs": false,
    "show-special": true,
    "workspace-taskbar": {
      "enable": true,
      "update-active-window": true,
      "format": "{icon}",
      "icon-size": 18,
    },
  },

  "hyprland/window": {
    "max-length": 80,
    "separate-outputs": true,
  },

  "hyprland/submap": {
    "format": "{}",
    "tooltip": false,
  },

  "hyprland/language": {
    "format": "󰌓 {short}",
    "tooltip-format": "{long}",
    "on-click": "hyprctl switchxkblayout $(hyprctl devices -j | jq -r '.keyboards[] | select(.main == true) | .name') next",
  },

  // -------------------------------------------------------------------------
  // Module overrides with hyprctl commands
  // -------------------------------------------------------------------------

  "cpu": {
    "interval": 5,
    "format": " {usage}%",
    "states": {
      "warning": 75,
      "critical": 90,
    },
    "on-click": "~/.config/hypr/scripts/launch-app.py process_manager",
  },

  "memory": {
    "interval": 5,
    "format": " {}%",
    "states": {
      "warning": 75,
      "critical": 90,
    },
    "on-click": "~/.config/hypr/scripts/launch-app.py process_manager",
  },

  "network": {
    "interval": 5,
    "format-wifi": "󱚻  {signaldBm}",
    "format-ethernet": "󰈀 ",
    "format-disconnected": "󰤠",
    "tooltip-format": "{ifname} ({essid}): {ipaddr}",
    "on-click": "~/.config/hypr/scripts/launch-app.py network_manager",
  },

  "pulseaudio": {
    "scroll-step": 5,
    "format": "{icon} {volume}%{format_source}",
    "format-muted": " {format_source}",
    "format-source": "",
    "format-source-muted": "󰍭",
    "format-icons": {
      "headphone": "󰋋",
      "headset": "󰋎",
      "default": ["", "󰕾", ""],
    },
    "tooltip-format": "{icon} {volume}% {format_source}",
    "on-click": "~/.config/hypr/scripts/launch-app.py audio_mixer",
    "on-click-middle": "wpctl set-mute @DEFAULT_AUDIO_SINK@ toggle",
    "on-scroll-up": "wpctl set-volume @DEFAULT_AUDIO_SINK@ 5%+",
    "on-scroll-down": "wpctl set-volume @DEFAULT_AUDIO_SINK@ 5%-",
  },

  "clock": {
    "interval": 60,
    "format": "{:%H:%M}",
    "tooltip": true,
    "tooltip-format": "<big>{:%B %Y}</big>\n<tt>{calendar}</tt>",
    "on-click": "~/.config/hypr/scripts/launch-app.py calendar",
  },

  // Probe every known CPU sensor and take the first that exists: coretemp
  // temp1_input is Intel's "Package id 0", the 00:18.3 function is where
  // k10temp binds on AMD family 17h+ and its temp1_input is Tctl.
  //
  // hwmon-path-abs takes the device directory *without* the hwmon<N>
  // component, because that number is assigned in driver probe order and
  // moves between boots — coretemp has been both hwmon7 and hwmon8 on one
  // machine, where hwmon7 is now a USB-C power supply with no temp input.
  "temperature#cpu": {
    "hwmon-path-abs": [
      "/sys/devices/platform/coretemp.0/hwmon",
      "/sys/devices/pci0000:00/0000:00:18.3/hwmon",
    ],
    "input-filename": "temp1_input",
    "warning-threshold": 72,
    "critical-threshold": 78,
    "interval": 3,
    "format": "{icon} {temperatureC}°C",
    "format-icons": ["", "", "", "", "", "", "", ""],
    "tooltip": false,
    "on-click": "~/.config/hypr/scripts/launch-app.py sensors",
  },

  "bluetooth": {
    "format": "󰂯 {num_connections}",
    "format-disabled": "󰂲",
    "on-click": "~/.config/hypr/scripts/launch-app.py bluetooth",
    "on-click-right": "rfkill toggle bluetooth",
    "tooltip-format": "{}",
  },
}
