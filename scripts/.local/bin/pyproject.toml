[project]
name = "remsi"
version = "1.0.0"
description = "Remove silent + filler regions from video via ffmpeg."
requires-python = ">=3.14"
dependencies = [
  "click>=8.1",
  "rich>=13.7",
  "requests>=2.32",
]

[tool.uv]
python-preference = "only-system"
