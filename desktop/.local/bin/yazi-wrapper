#!/bin/sh

# If argument is a file, open parent directory and highlight the file
if [ -f "$1" ]; then
  exec env zsh -c "yazi --entry=\"$1\""
else
  # Otherwise just open the directory
  exec env zsh -c "yazi $*"
fi
