[Service]
# systemd-xdg-autostart-generator resolves the desktop entry's Exec against its
# own PATH, which has no ~/.local/bin, so a bare `obsidian` there becomes
# /usr/bin/obsidian and skips the shim that pins the electron39 runtime.
ExecStart=
ExecStart=%h/.local/bin/obsidian
