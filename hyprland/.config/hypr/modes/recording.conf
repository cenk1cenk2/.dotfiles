# vim:ft=hyprlang

### Recording Mode

bind = $mod SHIFT, R, submap, 🎥 Recording: (r) toggle | (p) pause | (o) open OBS | (q) stop | (s) speech→clipboard | (S) speech→type | (Q) kill speech | ESC exit

submap = 🎥 Recording: (r) toggle | (p) pause | (o) open OBS | (q) stop | (s) speech→clipboard | (S) speech→type | (Q) kill speech | ESC exit

# Toggle recording (start/stop)
bind = , r, exec, $recorder toggle
bind = , r, submap, reset

# Toggle pause/resume
bind = , p, exec, $recorder pause
bind = , p, submap, reset

# Open OBS window
bind = , o, exec, $recorder open
bind = , o, submap, reset

# Stop recording
bind = , q, exec, $recorder stop
bind = , q, submap, reset

# Speech-to-text to clipboard (wl-copy)
bind = , s, exec, $speech toggle clipboard
bind = , s, submap, reset

# Speech-to-text direct typing (ydotool)
bind = SHIFT, s, exec, $speech toggle type
bind = SHIFT, s, submap, reset

# Terminate waystt
bind = SHIFT, q, exec, $speech kill
bind = SHIFT, q, submap, reset

# Exit recording mode
bind = , escape, submap, reset

submap = reset
