# vim:ft=hyprlang

### Recording Mode

bind = $mod SHIFT, R, submap, 🎥 Recording: (r) toggle | (p) pause | (o) open OBS | (q) stop | (s) speech→type | (c) speech→clipboard | (Q) kill speech | (z) zoom | ESC exit

submap = 🎥 Recording: (r) toggle | (p) pause | (o) open OBS | (q) stop | (s) speech→type | (c) speech→clipboard | (Q) kill speech | (z) zoom | ESC exit

# Toggle recording (start/stop)
bind = , r, exec, $recorder toggle
bind = , r, submap, reset

# Toggle pause/resume
bind = , p, exec, $recorder pause
bind = , p, submap, reset

# Open OBS window
bind = , o, exec, $recorder open
bind = , o, submap, reset

# Stop recording
bind = , q, exec, $recorder stop
bind = , q, submap, reset

# Speech-to-text direct typing (ydotool)
bind = , s, exec, $speech toggle type
bind = , s, submap, reset

# Speech-to-text to clipboard (wl-copy)
bind = , c, exec, $speech toggle clipboard
bind = , c, submap, reset

# Toggle zoom
bind = , z, exec, hypr-zoom -easing=InOutCubic -interp=Linear -target 1.5
bind = , z, submap, reset

# Terminate waystt
bind = SHIFT, q, exec, $speech kill
bind = SHIFT, q, submap, reset

# Exit recording mode
bind = , escape, submap, reset

submap = reset
