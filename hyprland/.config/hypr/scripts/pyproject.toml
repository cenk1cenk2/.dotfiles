[project]
name = "hyprland-scripts"
version = "1.0.0"
description = "Hyprland helper scripts."
requires-python = ">=3.14"
dependencies = [
  "click>=8.1",
  "rich>=13.7",
]

[tool.uv]
python-preference = "only-system"
