# Wrong-password lockout. The stock policy (3 misses, 600 s lock) shuts
# greetd for ten minutes over a fumbled passphrase; ten tries with a one
# minute cooldown still stops an online guesser while staying livable at
# the login screen. `faillock --reset` clears an active lock by hand.
deny = 10
unlock_time = 60
