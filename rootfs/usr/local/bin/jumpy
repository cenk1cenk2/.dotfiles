#!/usr/bin/env bash

ARGS=("${@:2}")

case "$1" in
"sound-sink")
  $HOME/.config/sway/scripts/sound.sh "${ARGS[@]}"
  ;;
"display-profile")
  $HOME/.config/sway/scripts/display.sh "${ARGS[@]}"
  ;;
*)
  echo "Usage: $0 [sound|display]"
  ;;
esac
