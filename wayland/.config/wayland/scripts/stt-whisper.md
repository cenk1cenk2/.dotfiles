We reviewed the change, ran the tests, and opened a merge request. The service returned a 500 after about 200 milliseconds, so I rolled it back and read the logs. Configuration lives in src/config/settings.yaml and is loaded at startup. Run it with --dry-run first, then --verbose if it fails. We pinned version 2.14.3 and closed issue 412. The handler calls parse_request and returns a UUID; the retry_count field defaults to 3. Check the API, the CLI, the HTTP timeout, and the TLS certificate on api.example.com. Is that 16 gigabytes or 16 gibibytes?
